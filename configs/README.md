# configs/

- `settings.yaml` — non-secret configuration: city bounding box, ward list, model artifact paths, API config
- `.env.example` — documents every required environment variable with a placeholder value, never a real secret

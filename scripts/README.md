# scripts/

One-command operational scripts:

- `setup_env.sh` — bootstraps the full dev environment (backend + frontend), idempotent
- `run_pipeline.sh` — runs the data pipeline stages in order
- `train_models.sh` — retrains the forecast and attribution models

# frontend/src/

- `components/` — one file per UI component
- `hooks/` — one data-fetching hook per API resource
- `types/` — TypeScript interfaces mirroring `backend/app/models/schemas.py`
- `lib/` — typed API client and formatting helpers
- `App.tsx` — top-level layout

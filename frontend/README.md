# frontend/

React + TypeScript + Leaflet decision console. Single-screen UI: a map plus a briefing panel, no multi-tab dashboard.

Talks only to the backend API — never fetches from external data sources directly.

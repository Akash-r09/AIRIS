# backend/app/models/

Pydantic schemas that define the API request/response contract.

This is the single source of truth for API shapes. `frontend/src/types/schemas.ts` mirrors this file by hand — any change here must be applied there in the same commit.

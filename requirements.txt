# AIRIS backend — Milestone 0 dependencies only.
# Packages for the data pipeline, ML training, and LLM briefing are added
# in their respective milestones (see docs/architecture.md, Part 3), not here.

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Config / settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
PyYAML==6.0.2
